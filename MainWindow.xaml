<Window x:Class="CrackSegmentationApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CrackSegmentationApp"
        xmlns:vm="clr-namespace:CrackSegmentationApp.ViewModels"
        mc:Ignorable="d"
        Title="Crack Segmentation - OmniCrack30k"
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Image Border Style -->
        <Style x:Key="ImageBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Image Label Style -->
        <Style x:Key="ImageLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0,15,0,0"/>
        </Style>

        <!-- BooleanToVisibilityConverter -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- Main content -->
            <RowDefinition Height="Auto"/> <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="15,10">
                <TextBlock Text="🔬" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="OmniCrack30k Segmentation" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,30,0"/>

                <Button Content="📁 Load Image" Command="{Binding LoadImageCommand}" />
                <Button Content="▶️ Run Segmentation" Command="{Binding RunSegmentationCommand}"
                        Background="#4CAF50"/>
                <Button Content="💾 Save Results" Command="{Binding SaveResultsCommand}"
                        Background="#FF9800"/>
            </StackPanel>
        </Border>

        <!-- Main Grid: 2x2 image display -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Original Image -->
            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource ImageBorderStyle}">
                <Grid>
                    <TextBlock Text="Input Image" Style="{StaticResource ImageLabelStyle}"/>
                    <Viewbox Margin="10">
                        <Image Source="{Binding OriginalImage}" Stretch="Uniform"/>
                    </Viewbox>
                    <!--<TextBlock Text="Load an image to begin"
                               FontSize="18"
                               Foreground="#999999"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding IsImageLoaded, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>-->
                </Grid>
            </Border>

            <!-- Softmax Probability -->
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource ImageBorderStyle}">
                <Grid>
                    <TextBlock Text="Softmax Probability" Style="{StaticResource ImageLabelStyle}"/>
                    <Viewbox Margin="10">
                        <Image Source="{Binding SoftmaxImage}" Stretch="Uniform"/>
                    </Viewbox>
                    <!--<TextBlock Text="Run segmentation to see results"
                               FontSize="18"
                               Foreground="#999999"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>-->
                </Grid>
            </Border>

            <!-- Argmax Mask -->
            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource ImageBorderStyle}">
                <Grid>
                    <TextBlock Text="Argmax Segmentation" Style="{StaticResource ImageLabelStyle}"/>
                    <Viewbox Margin="10">
                        <Image Source="{Binding ArgmaxImage}" Stretch="Uniform"/>
                    </Viewbox>
                    <!--<TextBlock Text="Run segmentation to see results"
                               FontSize="18"
                               Foreground="#999999"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>-->
                </Grid>
            </Border>

            <!-- Centerlines -->
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource ImageBorderStyle}">
                <Grid>
                    <TextBlock Text="Centerlines" Style="{StaticResource ImageLabelStyle}"/>
                    <Viewbox Margin="10">
                        <Image Source="{Binding CenterlinesImage}" Stretch="Uniform"/>
                    </Viewbox>
                    <!--<TextBlock Text="Run segmentation to see results"
                               FontSize="18"
                               Foreground="#999999"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>-->
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0">
            <Grid Margin="15,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusMessage}"
                           FontSize="14"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ProgressBar Width="200" Height="20" IsIndeterminate="True" Margin="0,0,10,0"/>
                    <TextBlock Text="Processing..." FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
